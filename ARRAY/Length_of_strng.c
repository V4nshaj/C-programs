#include <stdio.h>
#include <stdlib.h>/*
int main()
{
    int l=0;
    char string[]="I am an unschooler!";
    for(int i=0; string[i] != '\0'; i++) /*\0(backslash 0)-> NULL so it will chk whn the string ends i.e. NULL
    {                                 or while(string[i] != '\0')
        l++;
        printf("Length of the string = %d\n", l);
    return 0;
} */
