/*Functions are set of set of statements which perform specific task and can be called anywhere in the program for any number of times.
Types:
1)User-defined Function: Functions which are defined by user.eg main().
2)Pre-defined function: Functions which  are defined in header files and comes along with the function. eg scanf(),gets()
Function have 3 parts:
function prototype:
1)Function prototype: tell the compiler that a function with this specification will be present in program.
syntax: return-type function-name(parameter-list);
2)Function definition or declaration: what task the function will perform
syntax:
return-type function-name(parameter-list)
{
....
}
3)Function calling: Proving values to the function and telling it to perform task on these values and return the result to the user.
syntax: function-name(values)

*/
