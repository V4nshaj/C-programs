#include<stdio.h>/*
int swap(int a, int b)
{
    int temp;
    temp=a;
    a=b;
    b=temp;
    printf("After swapping a = %d b = %d\n", a, b);
    return 0;
}
int main()
{
    int x=5,y=7;

    printf("The value of x and y before func call are %d %d\n", x, y);
    swap(x, y);
    printf("\nx = %d y = %d \n", x, y);

    return 0;
}
*/
